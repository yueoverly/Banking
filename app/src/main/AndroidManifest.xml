<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".BankingApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BankingApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Splash Screen -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.BankingApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Register Activity -->
        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Main Activity (Customer) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Officer Main Activity -->
        <activity
            android:name=".activities.OfficerMainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Transfer Activity -->
        <activity
            android:name=".activities.TransferActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- OTP Verification Activity -->
        <activity
            android:name=".activities.OTPVerificationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Face Verification Activity -->
        <activity
            android:name=".activities.FaceVerificationActivity"
            android:exported="false" />

        <!-- Account Detail Activity -->
        <activity
            android:name=".activities.AccountDetailActivity"
            android:exported="false" />

        <!-- Transaction History Activity -->
        <activity
            android:name=".activities.TransactionHistoryActivity"
            android:exported="false" />

        <!-- Bill Payment Activity -->
        <activity
            android:name=".activities.BillPaymentActivity"
            android:exported="false" />

        <!-- Phone TopUp Activity -->
        <activity
            android:name=".activities.PhoneTopUpActivity"
            android:exported="false" />

        <!-- Map Activity -->
        <activity
            android:name=".activities.MapActivity"
            android:exported="false" />

        <!-- QR Scanner Activity -->
        <activity
            android:name=".activities.QRScannerActivity"
            android:exported="false" />

        <!-- Profile Activity -->
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false" />

        <!-- Create Customer Activity (for Officers) -->
        <activity
            android:name=".activities.CreateCustomerActivity"
            android:exported="false" />

        <!-- Manage Customers Activity (for Officers) -->
        <activity
            android:name=".activities.ManageCustomersActivity"
            android:exported="false" />

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_GOOGLE_MAPS_API_KEY" />

        <!-- Firebase Messaging Service -->
        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
